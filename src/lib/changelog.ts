export interface ChangelogEntry {
  version: string
  date: string
  features?: string[]
  fixes?: string[]
}

// Auto-generated by scripts/release.mjs — extracted from git commit messages.
export const CHANGELOG: ChangelogEntry[] = [
  {
    version: '1.3.10',
    date: '2026-05-14',
    features: [
      'Vision-AI PDF upload — text + drawings via Claude/GPT-4o',
    ],
    fixes: [
      'Parallelize Vision API calls, increase timeout, reduce image size',
    ],
  },
]

export function getCurrentChangelog(): ChangelogEntry {
  return CHANGELOG[0]
}
