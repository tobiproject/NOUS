export interface ChangelogEntry {
  version: string
  date: string
  features?: string[]
  fixes?: string[]
}

// Auto-generated by scripts/release.mjs — extracted from git commit messages.
export const CHANGELOG: ChangelogEntry[] = [
  {
    version: '1.3.11',
    date: '2026-05-14',
    fixes: [
      'Replace pdfjs with direct Supabase upload + Claude native PDF API',
      'serve pdfjs worker from /public — fixes main-thread block on Vercel',
      'GitHub Action permissions + explicit token for git push',
      'PDF upload — per-page progress, max 5 pages, 90s render timeout',
    ],
  },
]

export function getCurrentChangelog(): ChangelogEntry {
  return CHANGELOG[0]
}
