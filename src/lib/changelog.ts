export interface ChangelogEntry {
  version: string
  date: string
  features?: string[]
  fixes?: string[]
}

// Auto-generated by scripts/release.mjs — extracted from git commit messages.
export const CHANGELOG: ChangelogEntry[] = [
  {
    version: '1.3.59',
    date: '2026-05-17',
    features: [
      'RRR-Chart als horizontale Balken wie WinrateCharts — Datum/Asset/True-RR',
      'RRR-Trade-Liste, Logo-Fix Chrome, Update-Banner als zentriertes Modal',
      'Breakeven-Tabelle mit visuellen Balken — TQ-Balken + Break-Even-Marker',
      'RRR-Analyse Tab in Performance — Breakeven-Tabelle, Empfehlung, True-RR-Verteilung',
      'persist max_run_price + true_rr — auto-save mit Debounce, Wert bleibt beim Wiederholen',
      'Ziel-RRR in Simulation, Screenshot auto-select, Zwischenablage-Button im Chart',
      'Ziel-RRR in Risk-Konfiguration + Anzeige im Chart-Tab und Trade-Detail',
      'confirm dialog before deleting screenshot',
      'show trade reference data (entry, SL, TP, RR, Zeit) in Chart tab',
      'use saved screenshots in Simulation KI-Analyse — click to select, no re-upload needed',
      'screenshot UX overhaul + instant table indicator update',
      'delete + crop screenshots in lightbox, X-button on thumbnails',
      'wider dialog, bigger chart, clearer screenshot workflow, screenshots in Nachbereitung',
      'Switch chart screenshot to thum.io (no API key needed)',
      'Add server-side chart screenshot via ScreenshotOne API',
    ],
    fixes: [
      'Recharts Tooltip formatter type in RrrAnalyseTab',
      'remove auto-capture button from Chart tab',
      'lightbox with header/footer bars outside image — X, Zuschneiden, Löschen not overlaid',
      'replace trade.id with trade in saveCrop dependency array',
      'null-check for trade in deleteScreenshot and saveCrop',
      'intercept document.createElement to set iframe allow-permissions before load (Safari)',
      'Permissions-Policy header + MutationObserver for Safari clipboard/downloads',
      'grant clipboard-write + downloads permission to TradingView iframe',
      'add retina flag to thum.io for 2x resolution',
      'pass thum.io URL unencoded',
      'Use macOS screenshot shortcut instead of TV camera icon',
      'Revert to TV widget script with correct flex height fix',
      'Replace TV widget script with direct iframe embed',
      'use localStorage timestamp for skip-logout flag (survives iOS pagehide boundary)',
      'AutoLogout only on iOS + fix screenshot content-type for Safari',
    ],
  },
]

export function getCurrentChangelog(): ChangelogEntry {
  return CHANGELOG[0]
}
