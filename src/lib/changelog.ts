export interface ChangelogEntry {
  version: string
  date: string
  features?: string[]
  fixes?: string[]
}

// Auto-generated by scripts/release.mjs — extracted from git commit messages.
export const CHANGELOG: ChangelogEntry[] = [
  {
    version: '1.3.13',
    date: '2026-05-14',
    fixes: [
      'PDF-Upload komplett neu: direkt zu Supabase + Claude liest PDF nativ (Text + Zeichnungen)',
      'Auto-Release GitHub Action repariert (Permissions + Token)',
    ],
  },
]

export function getCurrentChangelog(): ChangelogEntry {
  return CHANGELOG[0]
}
